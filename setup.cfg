[flake8]
max-line-length = 100
extend-ignore =
    # f-string without placeholders — common in print/log messages
    F541
    # line break before binary operator (conflicts with Black style)
    W503
    # whitespace before ':' in slices — Black formats this intentionally
    E203
